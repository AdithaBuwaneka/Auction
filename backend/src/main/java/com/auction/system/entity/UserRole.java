package com.auction.system.entity;

/**
 * User Role Enum
 * Defines the roles that users can have in the system
 */
public enum UserRole {
    /**
     * Regular user - can buy and sell items
     */
    USER,

    /**
     * Administrator - has full access to manage the system
     */
    ADMIN
}
